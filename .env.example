# Copy this file to `.env` and fill in your key.
# The app runs without it — the research stage falls back to the
# deterministic mode when GEMINI_API_KEY is missing.

GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.0-flash
PORT=3001